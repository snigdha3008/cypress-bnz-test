{
  "name": "cypress-bnz-test",
  "version": "1.0.0",
  "description": "Datacom assignment",
  "main": "cypress.config.js",
  "scripts": {
    "open": "./node_modules/.bin/cypress open",
    "test": "./node_modules/.bin/cypress run --config pageLoadTimeout=60000"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.7.0"
  }
}
